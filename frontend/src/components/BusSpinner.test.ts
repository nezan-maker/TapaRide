import { describe, expect, it } from 'vitest'

import BusSpinner from './BusSpinner'

describe('BusSpinner', () => {
  it('exports a valid component', () => {
    expect(BusSpinner).toBeTypeOf('function')
  })
})
